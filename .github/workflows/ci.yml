name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  checks:
    name: Repository checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run CI checks
        run: bash scripts/ci.sh

{
  "name": "duri",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @duri/web",
    "build": "npm run build --workspace @duri/web",
    "lint": "npm run lint --workspace @duri/web",
    "typecheck": "npm run typecheck --workspace @duri/web",
    "test": "npm run test --workspace @duri/web"
  },
  "engines": {
    "node": ">=20"
  }
}
